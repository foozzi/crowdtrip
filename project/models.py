from django.db import models
from django.utils import timezone

class Project(models.Model):
	# title = models.CharField(max_length=254, unique=False)
	# amount = models.IntegerField(max_length=15)
	# last_name = models.CharField(max_length=100)
	# avatar = models.ImageField(upload_to=settings.AVATAR_UPLOAD_DIR + '/%Y/%m/%d', blank=True)
	# email = models.EmailField(max_length=254, unique=True)
	# is_staff = models.BooleanField(default=False)
	# is_active = models.BooleanField(default=True)
	# date_joined = models.DateTimeField(default=timezone.now)
	# confirm_key = models.CharField(max_length=254, unique=True)
	# bio = models.TextField(max_length=2000)